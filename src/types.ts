export interface EventDetails {
      _id?: string;
      title: string;
      description: string;
      date: string;
      eventCreatedAt: string;
      location: string;
      invitedContacts: string[];
      attendees: string[];
}